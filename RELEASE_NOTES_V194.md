# Release Notes V194 — Safe Non-Accounting Improvements

## النطاق

تم تنفيذ تحسينات حماية وتشغيلية فقط. لم تتم إعادة كتابة أو تعديل أي دالة ترحيل محاسبي أو احتساب ربح أو تكلفة بضاعة أو حركة مخزون أو قيد حوالة أو منطق حذف وتعديل العمليات.

## التحسينات

أضيف زر «إنشاء نسخة احتياطية الآن» في إعدادات النظام. يستخدم النسخ الاحتياطي الأصلي لـ SQLite عبر `Connection.backup` وينشئ نسخة زمنية داخل مجلد `backups` دون تغيير قاعدة البيانات المفتوحة.

أضيف زر «فحص سلامة قاعدة البيانات» في إعدادات النظام. ينفذ `PRAGMA integrity_check` و`PRAGMA foreign_key_check` للقراءة فقط، ويعرض النتيجة دون تعديل البيانات.

تم الإبقاء على النسخ الاحتياطي التلقائي الموجود عند بدء التشغيل. كما بقي نظام الصلاحيات الفردية وسجل الرقابة وإعدادات الهوية كما هي في الإصدار السابق.

## التحقق

```text
PY_COMPILE=0
SAFE_IMPROVEMENTS_TEST=PASS
PASS=32
FAIL=0
ACCOUNTING_CODE_PATHS_CHANGED=0
```

تغييرات V194 محصورة في أدوات الحماية داخل إعدادات النظام، ولا تغيّر نتائج العمليات المحاسبية.
